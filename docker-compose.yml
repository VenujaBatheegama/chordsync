services:
  chordsync:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - chordsync_data:/data
    environment:
      - DATABASE_URL=file:/data/chordsync.db
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  chordsync_data:
